import { OpenProfileButton } from '../../../entities';

export const OpenProfileSettings: React.FunctionComponent = () => {
    return (
        <div className='open-profile-settings'>
            <OpenProfileButton />
        </div>
    )
}