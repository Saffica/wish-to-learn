import React from 'react';
import Logo from '../../features';

const Header = () => {
    return (
        <div className='wish-to-lern__header'>
            <Logo />
        </div>
    );
};

export default Header;
